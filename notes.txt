Projects started at - 23 july
..... till now I have learned about react-router-doms. How router workes to redirect in different pages.
24 july- created Navbar which is common for all pages . Javascript logic is applyed  here in useState function , where if usestate is true - user is logged in , ekse logged out
25 july -
Then I understands about how components works. In Home.jsx page , we added till now three components in home - header  ,  steps and Testimonials
After I added Try.jsx at the bottom of the Home page.

always in onclick -  {()=>{}}

whenever made  login pop-up
 div>form
 div = absolute
 form = relative

 question need to be review -> Useeffect Hook case where scroll stopped when login/sign up appears

 27 july
 starting backend 
 initially- package instalation
   npm init
   npm i express cors dotenv nodemon form-data jsonwebtoken mongoose axios bcrypt razorpay

   UI colors.app


   usermodel : const userSchema = new mongoose.Schema({
  name: {
    type: String,
    required: true
  },
  email: {
    type: String,
    required: true,
    unique: true
  },
  password: {
    type: String,
    required: true
  },
  creditBalance: {
    type: Number,
    default: 5
  }
});

const userModel = mongoose.models.user || mongoose.model("user", userSchema);

in userController.js : Salt is a random value that bcrypt automatically adds to each password before hashing, ensuring that identical passwords produce completely different hashes for security.